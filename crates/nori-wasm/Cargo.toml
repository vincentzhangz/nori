[package]
name = "nori-wasm"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
description = "WASM bindings for the Nori compiler"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
nori = { path = "../nori" }
wasm-bindgen = "0.2"

[features]
default = []
